'use strict';

/**
 * business-consulting-hero-slider service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::business-consulting-hero-slider.business-consulting-hero-slider');
